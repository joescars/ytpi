# testing runner on new server
# 
name: ytpi-workflow

on: workflow_dispatch
jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Create Python virtual environment
        run: |
          cd /home/runneruser/services/ytpi
          /usr/bin/python3.11 -m venv venv

      - name: be Copy files to folder
        run: |
          cp -r ${{github.workspace}}/* /home/runneruser/services/ytpi
      
      - name: Install dependencies
        run: |
          cd /home/runneruser/services/ytpi
          source venv/bin/activate
          pip install --upgrade pip
          pip install -r requirements.txt
          pip install --upgrade yt-dlp
      - name: Restart Service
        run: |
          sudo systemctl restart ytpi.service
          sleep 5
          sudo systemctl status ytpi.service


